import React from 'react';
import { Typography, Box } from '@mui/material';

export default function Transacciones(): React.JSX.Element {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Transacciones
      </Typography>
      {/* Aquí puedes poner una tabla, una lista, etc. */}
    </Box>
  );
}